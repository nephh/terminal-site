{
  "name": "terminal-site",
  "version": "1.0.0",
  "description": "",
  "main": "adventure.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ./script.js --bundle --outfile=out.js --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.5"
  }
}
